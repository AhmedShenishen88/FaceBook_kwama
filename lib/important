if you want run  cloud_firestore: ^2.5.3

Go to android/app/build.gradle and add the following lines of codes:

make this:
dependencies {
  implementation 'com.android.support:multidex:1.0.3' //enter the latest version
}
android {
    defaultConfig {
        multiDexEnabled true
    }
}