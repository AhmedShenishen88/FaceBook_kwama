import 'package:flutter/material.dart';

class MarketPlacecreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text(
          'MarketPlace Screen',
          style: TextStyle(fontSize: 25, color: Colors.black),
        ),
      ),
    );
  }
}
